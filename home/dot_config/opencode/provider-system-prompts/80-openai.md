---
modelRegex: "^.+/gpt.*-5\\.[0-9].*$"
---

{{opencode:provider}}

Prefer shell commands over writing Python or other in-line scripts when tasks are commonly done in a shell (for example, base64 encoding/decoding, json parsing, etc).
