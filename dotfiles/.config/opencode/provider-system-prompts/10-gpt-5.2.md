---
modelRegex: "^.+/gpt-5\\.2$"
---

{{opencode:provider}}

Prefer shell commands over writing Python when tasks are commonly done in a shell (for example, base64 encoding/decoding).

If you need to do any coding that isn't a trivial script or change, write a plan and spec and then ask codex to implement it.
